services:
  ci-node20:
    build:
      context: .
      target: node20
    command: sh -c "npm run typecheck && npm run lint && npm run test:coverage && npm run build"

  ci-node22:
    build:
      context: .
      target: node22
    command: sh -c "npm run typecheck && npm run lint && npm run test:coverage && npm run build"
